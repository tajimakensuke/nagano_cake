<div class " mx-auto m-3" >
  <h2>商品一覧</h2>
</div>


    <div class = "row">
        <div class = "col-4">
            <div><%= attachment_image_tag @item, :image, :fill, 250, 250, format: 'jpeg'%></div>
        </div>

      <div class = "col-4">
          <div><%= @item.name %></div>
          <div><%= @item.introduction %></div>
          <div>￥<%= @item.price %>(税込)</div>
      </div>
    </div>